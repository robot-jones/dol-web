import { DolColor } from "@erikmuir/dol-lib/types";

export type BaseAttributeProps = {
  label?: string;
  textColor?: DolColor;
  attributeColor?: DolColor;
  fullWidth?: boolean;
  loading?: boolean;
};
